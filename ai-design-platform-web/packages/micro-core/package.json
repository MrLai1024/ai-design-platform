{
  "name": "@ai-design/micro-core",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'micro-core package — no build needed'",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ --ext .ts"
  },
  "dependencies": {
    "@ai-design/shared": "workspace:*",
    "qiankun": "^2.10.16"
  },
  "devDependencies": {
    "vitest": "^1.2.0"
  }
}
