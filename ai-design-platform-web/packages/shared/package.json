{
  "name": "@ai-design/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'shared package — no build needed'",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ --ext .ts"
  },
  "devDependencies": {
    "vitest": "^1.2.0"
  },
  "dependencies": {
    "axios": "^1.18.0"
  }
}
