package store

import "time"

// fixedUserID 是测试用的固定用户 id。
const fixedUserID = "11111111-1111-1111-1111-111111111111"

// fixedCreatedAt 是测试用的固定创建时间。
var fixedCreatedAt = time.Date(2026, 8, 25, 12, 0, 0, 0, time.UTC)
